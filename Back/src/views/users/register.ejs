<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('../partials/head')%>
        <link rel="stylesheet" href="css/registerstyles.css">
</head>

<body>
    <%- include('../partials/header')%>

        <main>
            <div id="registro">
                <form action="/register" method="post" enctype="multipart/form-data">
                    <h1>Registrate</h1>
                    <% if (typeof(errors) !='undefined' && errors.email) {%>
                        <div class="alert alert-danger">
                            <%= errors.email.msg %>
                        </div>
                        <% } %>
                            <% if (typeof(errors) !='undefined' && errors.password) {%>
                                <div class="alert alert-danger">
                                    <%= errors.password.msg %>
                                </div>
                                <% } %>
                                    <div class="form-group">
                                        <!--Noten aqui en los input estoy persistiendo los datos si el usuario coloca algunos datos  buenos, pero otros no-->
                                        <input type="text" class="form-control" name="nombre" placeholder="Nombre"
                                            value="<%= typeof old != 'undefined' ? old.nombre : '' %>">
                                    </div>
                                    <div class="form-group">
                                        <input type="text" class="form-control" name="apellido" placeholder="Apellido"
                                            value="<%= typeof old != 'undefined' ? old.apellido : '' %>">
                                    </div>
                                    <div class="form-group">
                                        <input type="email" class="form-control" name="email" placeholder="Email"
                                            value="<%= typeof old != 'undefined' ? old.email : '' %>">
                                    </div>
                                    <div class="form-group">
                                        <input type="password" class="form-control" name="password"
                                            placeholder="Contraseña">
                                    </div>
                                    <div class="form-group">
                                        <input type="password" class="form-control" name="confirm_password"
                                            placeholder="Confirma Contraseña">
                                    </div>
                                    <div class="custom-file">
                                        <input type="file" name="avatar" placeholder="Debes eligir tu avatar...">
                                    </div>
                                    <div>
                                        <div id="boton"><button type="submit"
                                                class="btn btn btn-info">Registrarme</button>
                                        </div>
                </form>
                <p>¿Ya tenés cuenta?<a href="/login">Iniciá sesión aquí</a></p>
            </div>
        </main>
        <%- include('../partials/footer')%>
        <script src="/js/User/ValidatorRegister.js"></script>
</body>

</html>